## 测试基础设施与稳定性增强 (2026-03-21)

- **摘要**: 引入了 Playwright E2E 测试框架，全面补齐核心 Hooks 单元测试，并增强了 SCM 模块的错误处理。
- **原因**: 提升项目在复杂异步流式交互下的稳定性，确保核心业务链路（登录、任务创建、代码 Diff 展示）不被破坏。
- **影响**:
    - `package.json`: 修复了测试路径，新增 `test:e2e` 命令。
    - `playwright.config.ts`: 完成 E2E 基础设施搭建。
    - `src/lib/scm.ts`: 增强环境变量兼容性及错误码透传。
    - `test/lib/scm.test.ts`: 新增 SCM 模块全量单元测试。
    - `test/hooks/`: 补齐了连接管理、仓库筛选、消息水合、沙盒初始化等核心 Hooks 的逻辑测试。
    - `e2e/`: 新增登录、Portal 交互及 Thread Chat 生命周期全链路测试。
- **验证**:
    - 运行 `npm run test:lib` 和 `npm run test:hooks`，29 个单元测试全部通过。
    - 运行 `npm run test:e2e`，通过 `smoke` 冒烟测试验证了基础路由和渲染。

## 文档体系重构与 Agent 协议化 (2026-03-21 补更)

- **摘要**: 对项目文档进行了深度重构，建立了“协议驱动”的 Agent 协作模式。
- **原因**: 根除冗余文档，提升 Agent 交付质量，建立硬性技术约束（拆分阈值、禁令等）。
- **主要改动**:
    - **AGENTS.md**: 精简为核心协议索引，引入 500/300 行拆分阈值及 Anti-Patterns 约束。
    - **CLAUDE.md**: 更新为 local 开发上下文，与 AGENTS.md 保持双向关联。
    - **docs/development/conventions.md**: 抽取并细化了异步加载、工具链折叠等核心交互规范。
    - **docs/changelogs/**: 将变更记录从协议中剥离，按日期细粒度拆分存储。
    - **doc-manager.skill**: 创建并安装了专门用于维护该协议体系的技能。
- **验证**: 执行了目录结构检查，确认所有跨文档链接有效，并在 AGENTS.md 中新增了文档闭环 Mandates。
