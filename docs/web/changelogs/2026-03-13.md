    - `src/hooks/thread-chat-diff-utils.test.ts`
    - `src/hooks/use-thread-chat.ts`
    - `src/lib/sandbox.ts`
    - `src/pages/task-detail.tsx`
    - `src/business/thread-chat/thread-chat-message-item.tsx`
    - `src/business/thread-chat/thread-chat-file-change-summary.tsx`
- 2026-03-13（Prompt 附件能力收敛 + 任务详情首屏加载判定补齐）：
  - 门户页与任务详情页 Prompt 输入统一收敛为“文本/配置/表格/常见代码文件”附件能力；移除图片导向文案，并将白名单与错误提示抽到共享模块，避免两处规则漂移。
  - `PromptInput` 的 `accept` 匹配补齐扩展名分支，避免 `.ts/.tsx/.md` 等文件因 MIME 不稳定而无法选择。
  - 任务详情页将 `git diff` 首次加载纳入首屏 loading 判定，避免会话区在 diff 尚未完成初始化时过早进入稳定态。
  - 工具执行中的分组标题与行内任务项统一改为 shimmer 执行态，减少布局抖动并保持加载反馈连续。
  - 本次前端关键改动文件：
    - `src/lib/prompt-input-attachments.ts`
    - `src/components/ai-elements/prompt-input.tsx`
    - `src/business/portal/portal-prompt-panel.tsx`
    - `src/business/thread-chat/thread-chat-input-panel.tsx`
    - `src/pages/task-detail.tsx`
    - `src/business/thread-chat/thread-chat-message-item.tsx`
    - `src/business/thread-chat/thread-chat-tool-group.tsx`
- 2026-03-05（会话流回归问题加固：重复消息 / 重复 key / 工具假 loading / token 归零）：
  - 修复同轮次 AI 消息重复：流式快照出现重复 `message.id`（如 `lc_run--...`）时，按 `message.id` 去重并保留最新消息，避免“同一条 AI 回复重复显示”。
  - 明确重复 key 根因治理：优先修复消息层重复，再使用稳定 `message.id` 作为渲染 key；禁止通过 `sourceIndex` 等易变字段拼 key 掩盖问题。
  - 修复工具执行完成后仍 loading：工具区执行态仅由 `tool.status === running` 驱动，不再由整轮 `isResponding` 推导，避免“工具已结束但还在转圈”。
  - 修复流式阶段上下文统计闪回 0：过滤占位 usage（`input/output/total` 全 0），并在流式缺失 usage 时沿用最近有效统计，避免 `上下文/输出` 在流中归零。
  - 本次前端关键改动文件：
    - `src/hooks/use-thread-chat.ts`
    - `src/business/thread-chat/thread-chat-message-list.tsx`
