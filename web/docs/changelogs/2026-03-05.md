    - `src/business/thread-chat/thread-chat-tool-group.tsx`
    - `src/business/thread-chat/thread-chat-message-item.tsx`
- 2026-03-05（前端本地 smoke 命令补齐：SCM 连接中心 + 线程级取消）：
  - 新增 `pnpm smoke:scm-connections`，可对 `connections/validate/revoke` 执行本地化 smoke（默认只读，`--revoke true` 才执行真实断开）。
  - 新增 `pnpm smoke:thread-cancel`，用于快速验证 `POST /sandbox/threads/{thread_id}/cancel` 行为，并在 `404/405` 时给出回退提示。
  - 本次前端关键改动文件：
    - `scripts/smoke-scm-connections.mjs`
    - `scripts/smoke-thread-cancel.mjs`
    - `package.json`
- 2026-03-04（会话发送时底部自动置底优化）：
  - 优化任务详情会话区发送体验：当滚动条已处于底部时，用户发送消息后自动保持置底。
  - 增加“发送前是否在底部”判定，避免用户上滑浏览历史消息时被强制拉回底部。
  - 本次前端关键改动文件：
    - `src/business/thread-chat/thread-chat-message-list.tsx`
- 2026-03-04（会话工具链流式加载态与稳定性修复）：
  - 修复工具消息在流式执行阶段的基础交互回归：回复状态存在但工具区 loading 缺失的问题。
