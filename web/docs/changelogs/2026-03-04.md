  - 约束当前轮 `tool` 消息在 streaming 阶段持续展示执行态，避免“只见回复不见工具执行”。
  - 修复工具链“弹跳式”显示：稳定 `tool-group` 分组与组内项 key，避免流式增量触发重挂载与折叠区抖动。
  - 本次前端关键改动文件：
    - `src/business/thread-chat/thread-chat-message-list.tsx`
    - `src/business/thread-chat/thread-chat-message-item.tsx`
    - `src/business/thread-chat/thread-chat-message-utils.ts`
    - `src/business/thread-chat/thread-chat-tool-group.tsx`
- 2026-03-04（AI 回复复制范围收敛）：
  - 修复任务详情页 AI 消息复制范围：复制 assistant 回复时不再拼接工具链/环境等中间消息内容，仅保留最终回复正文（`content` 分片）。
  - 保持用户消息复制行为不变；assistant 若无最终正文则不提供复制内容，避免复制到工具过程噪声。
  - 本次前端关键改动文件：
    - `src/business/thread-chat/thread-chat-message-utils.ts`
- 2026-03-04（会话失败消息顺序与样式优化）：
  - 修复连续失败场景的消息归属顺序：多次发送且均失败时，必须按轮次展示为“提问 -> 失败消息 -> 提问 -> 失败消息”，禁止聚合到末尾形成“提问 -> 提问 -> 失败 -> 失败”。
  - 失败归属策略改为“按未完成 user 轮次顺序匹配”，并在后端 human 消息 id 变化时按文本与顺序回填，确保失败消息挂载到正确用户提问后。
  - 消除重复错误提示：当会话内已存在逐轮失败消息时，不再额外展示底部全局红色流错误块。
  - 逐轮失败消息视觉样式统一为轻量 warning（amber）提示卡片，避免使用强烈 destructive 红框干扰阅读。
  - 本次前端关键改动文件：
    - `src/hooks/use-thread-chat.ts`
    - `src/business/thread-chat/thread-chat-panel.tsx`
    - `src/business/thread-chat/thread-chat-message-list.tsx`
    - `src/business/thread-chat/thread-chat-message-item.tsx`
- 2026-03-04（线程消息流异常事件渲染修复）：
  - 修复任务详情会话区对 `event: error` 的可见性问题：流通道返回异常事件后，异常信息必须在消息区可见，禁止静默吞错。
  - `TaskDetailPage` 调整为始终向会话面板透传 `error`，不再仅在“无消息”场景下传递，避免“有历史消息时异常不可见”。
  - `ThreadChatPanel` 增加流错误文本提取逻辑（兼容 `message/detail/error` 及嵌套 `error.message`），并显式传递给消息列表层。
  - `ThreadChatMessageList` 新增 `streamErrorMessage` 显式 props，并在 `chatStatus=error` 时渲染“消息流发生异常”提示块；空态与非空态均需可见。
  - 本次前端关键改动文件：
    - `src/pages/task-detail.tsx`
    - `src/business/thread-chat/thread-chat-panel.tsx`
    - `src/business/thread-chat/thread-chat-message-list.tsx`
- 2026-03-02（代码审查配置持久化与门户联动）：
  - 新增 `src/lib/code-review.ts`，封装 `/integrations/code-review/settings*` 接口读写与错误处理。
  - `CodeReviewSettings` 完成后端化：初始化加载“SCM 仓库 + 代码审查配置”，全局与仓库配置改为实时保存并支持失败回滚。
  - 接入 `webhook_sync` 反馈：自动同步成功提示，失败时提示手工配置指引。
  - 门户页 `autoCodeReview` 改为从后端全局配置读取，不再使用硬编码默认值。
  - “创建环境”弹窗提交时会同步保存目标仓库的自动审查开关（enabled/disabled），并在开启时跳转 `/settings` 聚焦对应仓库。
- 2026-03-02（会话流式体验与防抖优化）：
  - 发送消息后新增本地乐观用户消息，保证“用户消息 + 正在回复”同步出现。
  - “正在回复...”迁移到最新消息行动条展示，底部全局状态仅作为兜底。
  - 复制按钮策略调整为“仅禁用当前轮 assistant 消息”，避免全局禁用。
  - 修复列表与工具链 key 不稳定导致的历史消息 DOM 刷新与抖动。
