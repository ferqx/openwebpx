    - `src/business/thread-chat/thread-chat-message-utils.test.ts`
    - `src/business/thread-chat/thread-chat-message-utils.ts`
    - `src/business/thread-chat/thread-chat-tool-group.tsx`
    - `src/components/ai-elements/code-block.tsx`
    - `src/hooks/thread-chat-utils.test.ts`
    - `src/hooks/thread-chat-utils.ts`
    - `src/main.tsx`
- 2026-03-13（线程 diff 统计精度修复 + git diff 初始化态补齐）：
  - `apply_patch` 的变更行数统计改为基于 Myers shortest edit script 计算，避免大段重复 search/replace 场景下因 O(n*m) LCS 内存与精度问题导致统计失真。
  - `useThreadChat` 对 `apply_patch` 工具的摘要展示改为优先使用严格净变更统计；当无法可靠得出结果时保持未定义，禁止回退到易误导的粗略计数。
  - 任务详情页读取线程 `git changes` 时补齐 `pending_initialization` 协议字段，并仅在环境引导完成、线程检查完成且当前非提交/流式状态时刷新 diff，避免初始化中的 diff 请求过早打断首屏状态判定。
  - 工具任务项标题优先展示真实 `toolInvocation`，文件变更摘要卡片与消息正文间距同步微调，减少长工具链阅读时的视觉挤压。
  - 新增 `thread-chat-diff` 定向测试脚本，覆盖大体量重复 patch 的精确计数回归。
  - 本次前端关键改动文件：
    - `package.json`
    - `src/hooks/thread-chat-diff-utils.ts`
