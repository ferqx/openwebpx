  - 修复流式文本样式对 emoji/代码块渲染的干扰，避免回复中 emoji 变白。
- 2026-03-02（设置页 + 代码审查配置页）：
  - 新增受保护路由 `/settings`，页面由 `SettingsPage` 承载，使用 `PortalHeader` 与纵向 Tabs。
  - 设置页默认进入“代码审查”tab，并挂载 `CodeReviewSettings`。
  - `CodeReviewSettings` 接入 SCM 连接与仓库聚合加载：先 `fetchScmConnections`，再对有效连接并行 `fetchScmRepositories`。
  - 门户页 `review` tab 保持为引导态，按钮统一跳转设置页进行代码审查配置。
- 2026-03-02（认证接入：登录/注册 + 路由守卫）：
  - 顶层新增 `AuthProvider`，并通过 `RequireAuth / RedirectIfAuthenticated` 收敛未登录跳转与已登录回跳逻辑。
  - 新增 `/login` 与 `/register` 页面，登录/注册成功后自动写入 token 并回跳原目标路由（默认 `/`）。
  - `authFetch/withAuthHeaders` 统一注入 `Authorization`；前端 token 存储键为 `sandbox-agent:auth-token`。
  - 新增认证相关 API 约定：`/api/auth/login`、`/api/auth/register`、`/api/auth/me`、`/api/auth/logout`。
- 2026-03-01（任务取消：线程级 cancel 接入）：
  - 前端取消逻辑升级为“优先调用 `POST /sandbox/threads/{thread_id}/cancel`”，不再依赖先拿到活跃 `run_id`。
  - 保留后向兼容：若后端未提供新接口（`404/405`），自动回退到原有 `runs.list -> runs.cancel` 逻辑。
  - `TaskDetailPage` 与 `AppChat` 的输入区 Stop 按钮同步接入线程级取消，避免“只停前端流，不停后端 run”。
  - 修复取消后复制按钮误挂载：`finishReason=tool_calls` 的 assistant 中间态消息不再显示复制按钮。
  - 新增文档：`docs/sandbox-thread-cancel-recovery.md`，记录接入点、行为变化与回退策略。
- 2026-03-01（文档命令补充）：
  - 基于 `docs/scm-auth-troubleshooting.md` 增补 SCM 前端定向检查命令（定向 eslint + app tsconfig 快速类型检查），用于授权链路回归前的快速自检。
- 2026-03-01（SCM 授权闭环增强）：
  - 前端授权状态真源切换为后端连接中心（`/integrations/scm/connections`）；移除本地 `localStorage` 授权状态作为真源的策略。
  - 门户仓库刷新链路调整为“先同步连接状态，再按来源聚合拉仓库”，确保 GitHub/GitLab/GitLab 私有可并存展示且不会互相覆盖。
  - 授权弹窗新增“已连接来源”区块，支持逐来源断开授权（调用 `DELETE /integrations/scm/connections`）。
